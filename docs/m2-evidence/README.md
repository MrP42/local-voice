# M2 evidence

Artifacts from `apps/local-voice/scripts/m2-verify.ps1`, run against the release
binary with the real microphone, the real global hotkey and real Notepad.

`notepad-*.txt` are read back out of Notepad through UI Automation, i.e. they are
what the user would actually see on screen, not what the app logged.
